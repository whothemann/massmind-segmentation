# Tested with Python 3.14.2 on macOS arm64.
# Bounds are loose; the project works on any recent NumPy 2.x / albumentations 2.x /
# torch 2.10+ combo. Pin tighter if you need byte-identical reproducibility.

# Core numerics / vision
numpy>=2.0
opencv-python-headless>=4.10
Pillow>=10.0
tifffile>=2024.5
scikit-image>=0.24

# PyTorch
torch>=2.4
torchvision>=0.19

# Augmentation
albumentations>=2.0

# Models
segmentation-models-pytorch>=0.5
timm>=1.0
transformers>=4.40         # only needed once SegFormer comes in; cheap to install now

# Data / IO / config
gdown>=6.0
PyYAML>=6.0
tqdm>=4.66

# Notebooks
matplotlib>=3.8
nbclient>=0.10
jupyterlab>=4.0
seaborn>=0.13

# Tests
pytest>=8.0
